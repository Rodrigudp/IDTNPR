# Perfil de produção.
# Todas as credenciais devem vir de variáveis de ambiente (nunca commitadas).
spring:
  jpa:
    properties:
      hibernate:
        show_sql: false

logging:
  level:
    root: INFO
    br.org.idtnpr: INFO
